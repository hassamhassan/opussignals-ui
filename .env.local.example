# Base URL of the OpusSignals FastAPI backend (no trailing slash).
# Copy to `.env.local`. In production (Amplify) set this to https://api.opussignals.com.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
